# Sample audio

Add `.mp3`, `.wav`, or `.m4a` files here if you want to host local demo audio. The preset buttons in the frontend use placeholder HTTPS URLs by design; replace those URLs in `frontend/src/components/UploadPanel.jsx` with publicly reachable files before a live presentation.
